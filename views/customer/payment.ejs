<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
		<link rel="stylesheet" href="/customer_menu.css" />
		<title>Payment</title>
	</head>
	<body>
		<div class="container py-5">
			<div class="position-relative mb-4">
				<h1 class="text-center">Order Summary</h1>
				<a href="/customer" class="btn btn-outline-primary position-absolute top-0 end-0" id="back-button">Back to Restaurants</a>
			</div>

			<div class="summary">
				<table class="table table-responsive order-table">
					<tr>
						<th><h3>Dish</h3></th>
						<th><h3>Quantity</h3></th>
						<th><h3>Price</h3></th>
					</tr>
					<% let price = 0; mergedItems.forEach((item) => { %>
					<tr>
						<td><h5><%= item.dish_name %></h5></td>
						<td><h5><%= item.quantity %></h5></td>
						<td><h5>$<%= (item.price * item.quantity).toFixed(2) %></h5></td>
					</tr>
					<% price += item.price * item.quantity; %> <% }); %>
				</table>
				<br>
				<div class="bill">
					<div class="delivery-fee">Delivery Fee: $10.00</div>
					<div class="total">Total: $<%= (price + 10).toFixed(2) %></div>
					<br>
					<form style="display: inline" action="/customer/pay" method="post">
						<input type="hidden" name="orderId" value="<%= orderId %>" />
						<button class="pay-btn" type="submit">Pay Now</button>
					</form>
				</div>
			</div>
		</div>
	</body>
</html>
