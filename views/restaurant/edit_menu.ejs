<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<title>Edit Menu Item</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
		<link rel="stylesheet" href="/restaurant.css" />
	</head>
	<body>
		<div class="container py-5">
			<div class="position-relative mb-4">
			<h1 class="text-center">Edit Menu Item</h1>
			</div>

			<div class="management-edit">
				<form action="/restaurant/menu/edit/<%= menu.id %>" method="post" class="edit-form">
					<input type="hidden" name="restaurant" value="<%= menu.restaurant %>" />

					<label>Dish Name:</label>
					<input
						type="text"
						name="dish_name"
						value="<%= menu.dish_name %>"
						required />

					<label>Price: </label>
					<input
						type="number"
						name="price"
						value="<%= menu.price %>"
						step="0.01"
						required />

					<button
						type="button"
						id="cancel-button"
						onclick="window.location.href='/restaurant?name=<%= encodeURIComponent(menu.restaurant) %>'">
						Cancel
					</button>
					<button type="submit" class="change-button">Save Changes</button>
				</form>
			</div>
		</div>
	</body>
</html>
